export type AboutSectionContent = {
    title: string;
    description: string;
    buttonText?: string;
  };

export type OrganizationSectionContent = {
    title: string;
};

export type SponsorSection = {
  title: string;
}
